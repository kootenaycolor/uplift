# Credentials and tokens — never commit these
credentials.json
token.json

# Build artifacts — DMGs go to GitHub Releases, not the repo (>100 MB limit)
dist/
build/
*.spec
launcher_stub.c

# Backup files from dev sessions
main_before_*.py
config_before_*.py

# Python cache
__pycache__/
*.pyc

# macOS
.DS_Store
